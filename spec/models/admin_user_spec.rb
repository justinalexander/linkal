require 'spec_helper'

describe AdminUser do
  #currently this spec describes a basic ActiveRecord model but keeping file if unit tests become necessary
  #This model is also just using the default ActiveAdmin options so far so no unit testing needed.
end
