%h2 My Organizations
%ul#events
  - @venues.each do |venue|
    %li{"data-role" => :collapsible, "data-theme" => "c"}
      %h5.ui-li-header
        = venue.name
      %p.ui-li-desc
        %form
          %input#company_only{:type => :checkbox, "data-theme" => :d, "data-mini" => :true}
          %label{:for => :company_only} Company Only Events

%p
  = link_to 'Follow more organizations', { :controller => :settings, :action => :follow },
    "data-role" => "button", "data-mini" => true, "data-inline" => "true"
